export const metadata = {
  title: {
    default: "PM-Dashboard",
    template: "%s | Co-Work",
  },
  description: "Project Clarity Dashboard",
};

export default function ProjectManagerLayout({ children }) {
  return children;
}
